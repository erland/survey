project: survey-service
mode: CREATE
current_step: STEP-35
current_step_name: Deployment profile
status: implemented_static_verified
completed_through: STEP-35
verification_policy:
  local_static: required
  runtime: deferred_to_ci_when_tooling_unavailable
history_summary:
  - STEP-01..03: bootstrap, CI, PostgreSQL and Flyway
  - STEP-04..08: admin authentication, survey domain/API and editor
  - STEP-09..13: survey-run snapshot, lifecycle, public lookup and sharing
  - STEP-14..18: anonymous participant session, participant UI, autosave, submit and heartbeat
  - STEP-19..25: result aggregation, live/SSE, charts, free text, presentation and read-only token
  - STEP-26..30: survey/result import-export and complete archival package
  - STEP-31..33: UX, accessibility and security hardening
  - STEP-34: Playwright end-to-end coverage and CI E2E job
step_35:
  changes:
    - production Compose profile with frontend, backend and PostgreSQL services
    - multi-stage Dockerfiles for Quarkus/Java 21 and React/Nginx
    - same-origin Nginx reverse proxy for /api
    - Nginx proxy settings disable buffering/caching and extend timeouts for SSE
    - frontend and backend healthchecks plus PostgreSQL readiness dependency
    - persistent PostgreSQL volume; app containers remain stateless
    - production environment example with no usable default passwords
    - PostgreSQL backup and restore scripts using pg_dump/pg_restore custom format
    - deployment documentation for HTTPS proxy, SSE, upgrades, backup/restore and bootstrap admin
  verification:
    - YAML files parse successfully
    - shell scripts pass sh syntax check
    - Dockerfiles and Nginx config statically reviewed
    - runtime container build and reverse-proxy SSE test deferred because Docker/Podman is unavailable locally
  notes:
    - only frontend port is exposed by the production Compose profile
    - outer reverse proxy must sanitize forwarded client-IP headers and terminate HTTPS
    - remove bootstrap admin credentials after first administrator creation
next_step: STEP-36
next_step_name: Release candidate
